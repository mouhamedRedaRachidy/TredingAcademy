CREATE TABLE news(
	id int(3) PRIMARY KEY AUTO_INCREMENT,
    photo varchar(100)

);
CREATE TABLE users(
    id int(11) PRIMARY KEY AUTO_INCREMENT,
    username varchar(30),
    password varchar(30)
    
);